## Quick start
1) Install Python 3.10+ and Google Chrome
2) Create and activate a virtual env
3) `pip install -r requirements.txt`
4) `python gmb_scraper.py` to scrape
5) 'python send_whatsapp.py' to run the whatsapp automation.
